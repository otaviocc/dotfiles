# bat configuration — one CLI argument per line.
#
# "Catppuccin Mocha" is built into bat (>= 0.25), so it resolves by name alone
# — nothing is vendored here and no `bat cache --build` is needed. Confirm with
# `bat --list-themes | grep Catppuccin`. Palette: see docs/palette.md.

--theme="Catppuccin Mocha"

# Line numbers, git modification markers and a header per file, but no grid.
--style="numbers,changes,header"

# Only page when the output doesn't fit; keep colors and let bat's default
# less flags through.
--paging=auto
